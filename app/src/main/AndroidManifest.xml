<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.example.bogoods">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".page.ListBarang" android:parentActivityName=".page.Store">

        </activity>
        <activity
                android:name=".page.FillData"
                android:parentActivityName=".page.ChooseAs" />
        <activity android:name=".page.Login" />
        <activity
                android:name=".page.Store"
                android:parentActivityName=".page.Dashboard" />
        <activity
                android:name=".page.EditProfile"
                android:parentActivityName=".page.Profile" />
        <activity
                android:name=".page.Profile"
                android:parentActivityName=".page.Dashboard" />
        <activity
                android:name=".page.Dashboard"
                android:label="@string/title_activity_dashboard_seller"
                android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".page.ChooseAs" />
        <activity android:name=".page.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>