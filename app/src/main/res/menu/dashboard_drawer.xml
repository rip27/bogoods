<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:tools="http://schemas.android.com/tools"
      tools:showIn="navigation_view">
    <group android:checkableBehavior="single">
        <item
                android:id="@+id/nav_profile"
                android:icon="@drawable/ic_person_black"
                android:title="@string/menu_profile"/>
        <item
                android:id="@+id/nav_store"
                android:icon="@drawable/ic_store_black"
                android:title="@string/menu_store"/>
        <item
                android:id="@+id/nav_your_order"
                android:icon="@drawable/ic_shopping_cart_black"
                android:title="@string/menu_your_order"/>
        <item
                android:id="@+id/nav_tools"
                android:icon="@drawable/ic_menu_manage"
                android:title="@string/menu_tools"/>
    </group>
</menu>
